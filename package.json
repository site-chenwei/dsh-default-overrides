{
  "name": "dsh-default-overrides",
  "private": true,
  "description": "Configurable Bash and PowerShell overrides for the DSH standard preset.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "check": "node --check scripts/dsh-default-overrides.mjs && node --check scripts/gitbash-executor.mjs && node --check scripts/verify-dsh-default-overrides.mjs",
    "verify": "node scripts/verify-dsh-default-overrides.mjs"
  }
}
